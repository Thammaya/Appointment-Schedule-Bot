{
  "name": "appointment-booking-webhook",
  "version": "1.0.0",
  "description": "Dialogflow CX webhook for medical appointment booking system",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=appointmentWebhook",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/bigquery": "^7.3.0"
  },
  "devDependencies": {},
  "keywords": [
    "dialogflow",
    "webhook",
    "bigquery",
    "appointment",
    "healthcare"
  ]
}
